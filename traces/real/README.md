# Real router traces

Real Qwen3 router traces are not part of the single-layer analytic milestone.
Each future trace set must include a manifest with model revision, dataset,
precision, inference framework, random seed and SHA-256 checksums.
