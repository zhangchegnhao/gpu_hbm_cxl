# AGENTS.md

## 项目背景

本项目用于从零构建我的科研实验环境。

我的研究方向是 **面向大语言模型（LLM）推理的存算一体（Processing-in-Memory，PIM）加速**，当前主要关注 **Mixture-of-Experts（MoE）模型的端到端 Decode 阶段**。

当前研究设想是一种三级异构架构：

```text
GPU
+
Local HBM-PIM
+
Shared CXL-PIM
```

其中：

- **GPU**：负责常规计算以及适合 GPU 执行的部分任务；

- **Local HBM-PIM**：与单个 GPU 紧耦合，具有高带宽、低延迟的特点；

- **Shared CXL-PIM**：作为多个 GPU 可以访问的系统级共享 PIM 资源，提供更大的共享容量和近数据计算能力。

研究的核心问题是：

> 在端到端 MoE LLM Decode 过程中，如何根据不同计算和访存任务的特点，在 GPU、Local HBM-PIM 和 Shared CXL-PIM 之间合理划分和调度工作负载，从而降低推理延迟并提高系统整体效率。

当前项目处于**实验环境从零搭建阶段**。

现阶段 Codex 只需要理解上述研究背景，不要默认已有具体实验框架、Simulator、Baseline、调度算法或实现方案。后续的实验环境、工具选择、系统模型和具体实现将根据我的指令逐步构建。

在后续工作中，应优先保证：

- 实验环境清晰、可复现；

- 不随意假设尚未确定的硬件模型和参数；

- 不提前实现尚未明确要求的研究功能；

- 每次只围绕当前明确的任务进行修改；

- 涉及科研模型和实验假设时，应先理解需求，再进行实现。
